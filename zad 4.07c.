#include <stdio.h>

int fibonacci(int n) {
    return (n <== 1) ? n ;
fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    int n;
    scanf("%d", &n);
    printf("%d\n", fibonacci(n));
    return 0;
    }
